# История исправлений багов

## 🐛 Баг #1: Некорректная обработка сообщений при переходе между меню

### Описание проблемы
Пользователь находится в меню "📍 Точка старта", вводит произвольный текст "спб пейзажная 16" (забыв нажать кнопку "📝 Ручной ввод"), и получает:
1. ❌ "Неверный формат времени. Используйте формат ЧЧ:ММ - ЧЧ:ММ"
2. ❓ "Используйте кнопки меню для навигации"

### Причина
Когда пользователь переходит из одного меню в другое (например, из редактирования заказа в меню маршрута), **состояние пользователя не очищается**.

**Сценарий воспроизведения:**
1. Пользователь редактирует заказ → нажимает "🕐 Время доставки"
2. Состояние устанавливается: `waiting_for_order_delivery_time`
3. Пользователь нажимает "⬅️ Главное меню" → "🗺️ Маршрут"
4. Состояние **НЕ ОЧИЩАЕТСЯ**
5. Пользователь вводит текст → бот думает что это время доставки

### Исправление
Добавлена очистка состояния в обработчики переходов между меню:

**Файл:** `src/bot/handlers/base_handlers.py`

```python
def handle_orders_menu(self, message):
    """Открыть меню заказов"""
    # Очищаем состояние при переходе в меню
    self.parent.clear_user_state(message.from_user.id)
    # ...

def handle_route_menu(self, message):
    """Открыть меню маршрута"""
    # Очищаем состояние при переходе в меню
    self.parent.clear_user_state(message.from_user.id)
    # ...

def handle_settings_menu(self, message):
    """Открыть меню настроек"""
    # Очищаем состояние при переходе в меню
    self.parent.clear_user_state(message.from_user.id)
    # ...

def handle_back_to_main(self, message):
    """Вернуться в главное меню"""
    # Очищаем состояние при переходе в главное меню
    self.parent.clear_user_state(message.from_user.id)
    # ...
```

### Статус
✅ **Исправлено** (15.12.2025 22:45)

---

## 🐛 Баг #3: Сломался ручной ввод адреса точки старта

### Описание проблемы
После исправления бага #1 перестал работать ручной ввод адреса точки старта:
1. Пользователь нажимает "✍️ Адрес"
2. Вводит адрес "спб пейзажная 16"
3. Получает "❓ Используйте кнопки меню для навигации"

### Причина
В `route_handlers.py` использовался **неправильный API** для установки состояния:

**Неправильно:**
```python
self.parent.update_user_state(user_id, 'waiting_for_start_address', {})
# Создает: user_states[user_id] = {'waiting_for_start_address': {}}
```

**Правильно:**
```python
self.parent.update_user_state(user_id, 'state', 'waiting_for_start_address')
# Создает: user_states[user_id] = {'state': 'waiting_for_start_address'}
```

В результате при вводе адреса `_handle_message_with_state` не находил состояние в ключе `'state'` и обрабатывал сообщение как неизвестное.

### Исправление
Исправлены все 6 вызовов `update_user_state` в `route_handlers.py`:

**Файл:** `src/bot/handlers/route_handlers.py`

```python
# Было:
self.parent.update_user_state(user_id, 'waiting_for_start_address', {})
self.parent.update_user_state(user_id, 'waiting_for_start_time', {})
self.parent.update_user_state(user_id, 'waiting_for_start_location', {'type': 'geo'})

# Стало:
self.parent.update_user_state(user_id, 'state', 'waiting_for_start_address')
self.parent.update_user_state(user_id, 'state', 'waiting_for_start_time')
self.parent.update_user_state(user_id, 'state', 'waiting_for_start_location')
self.parent.update_user_state(user_id, 'location_type', 'geo')  # дополнительные данные
```

### Статус
✅ **Исправлено** (15.12.2025 22:50)

---

## 🐛 Баг #4: Ошибка при сохранении точки старта (gis_id вместо start_time)

### Описание проблемы
При вводе адреса точки старта возникает ошибка:
```
psycopg2.errors.DatetimeFieldOverflow: date/time field value out of range: "70030076164727726"
```

### Причина
В `process_start_location` передавался `gis_id` (ID объекта из геокодера) вместо `start_time`:

**Неправильно:**
```python
lat, lon, gid = maps_service.geocode_address_sync(address)
self.parent.db_service.save_start_location(
    user_id, 'address', address, lat, lon, gid, today  # gid попадает в start_time!
)
```

### Исправление
Передаем `None` вместо `gid`, так как время старта вводится на следующем шаге:

**Файл:** `src/bot/handlers/route_handlers.py` (строка 284)

```python
self.parent.db_service.save_start_location(
    user_id, 'address', address, lat, lon, None, today  # start_time=None
)
```

### Примечание
Поле `gis_id` не используется в таблице `start_locations`. Если нужно сохранять GIS ID, требуется добавить новый столбец в модель.

### Статус
✅ **Исправлено** (15.12.2025 23:05)

---

## 🐛 Баг #5: Отсутствующий метод update_start_time

### Описание проблемы
При попытке установить время старта бот вызывал несуществующий метод:
```python
self.parent.db_service.update_start_time(user_id, start_datetime, today)
# AttributeError: 'DatabaseService' object has no attribute 'update_start_time'
```

### Причина
Метод `update_start_time` никогда не был реализован в `DatabaseService`, хотя использовался в `route_handlers.py` (строка 374).

### Исправление
Создан метод `update_start_time` в `DatabaseService`:

**Файл:** `src/services/db_service.py`

```python
def update_start_time(self, user_id: int, start_time: datetime, 
                     location_date: date = None, session: Session = None) -> bool:
    """Обновить время старта"""
    # Находит StartLocationDB за указанную дату и обновляет start_time
    # Возвращает True если успешно, False если точка старта не найдена
```

### Статус
✅ **Исправлено** (15.12.2025 23:10)

---

## 🐛 Баг #6: Убран функционал подтверждения адреса точки старта

### Описание проблемы
При исправлении бага #4 случайно был удален функционал подтверждения адреса. Адрес сразу сохранялся в БД без возможности проверить правильность геокодирования.

**Было (правильно):**
1. Пользователь вводит адрес
2. Бот геокодирует адрес
3. **Показывает inline кнопки: "✅ Да, верно" / "❌ Нет, ввести заново"**
4. После подтверждения сохраняет в БД

**Стало (неправильно):**
1. Пользователь вводит адрес
2. Бот геокодирует адрес
3. **Сразу сохраняет в БД** ❌
4. Переход к вводу времени

### Причина
При исправлении передачи `gid` был упрощен код и убран промежуточный шаг с подтверждением.

### Исправление
Восстановлен функционал подтверждения адреса:

**Файл:** `src/bot/handlers/route_handlers.py`

**1. После геокодирования сохраняем в состояние (не в БД):**
```python
self.parent.update_user_state(user_id, 'pending_location', {
    'address': address,
    'lat': lat,
    'lon': lon
})
self.parent.update_user_state(user_id, 'state', 'confirming_start_location')
```

**2. Формируем ссылки на карты для проверки:**
```python
dgis_link = f"https://2gis.ru/geo/{gid}?m={lon}%2C{lat}%2F17.87"
yandex_link = f"https://yandex.ru/maps/?whatshere[point]={lon},{lat}&whatshere[zoom]=17"
```

**3. Показываем сообщение со ссылками и inline кнопками:**
```python
self.bot.send_message(
    message.chat.id,
    f"📍 <b>Проверьте адрес точки старта</b>\n\n"
    f"<b>Адрес:</b> {address}\n"
    f"<b>Координаты:</b> {lat:.6f}, {lon:.6f}\n\n"
    f"🔗 <a href='{dgis_link}'>Открыть в 2ГИС</a> | "
    f"<a href='{yandex_link}'>Открыть в Яндекс Картах</a>\n\n"
    f"Правильно ли определен адрес?",
    parse_mode='HTML',
    reply_markup=markup,
    disable_web_page_preview=False  # Telegram покажет превью карт
)
```

**4. Добавлен обработчик отклонения:**
```python
def handle_reject_start_address(self, call):
    """Отклонение адреса - запрос повторного ввода"""
    # Возвращает в состояние ожидания адреса
```

### Преимущества
- ✅ Пользователь может проверить правильность распознанного адреса
- ✅ Видит координаты до сохранения
- ✅ **Может открыть адрес на картах (2ГИС, Яндекс Карты) с превью**
- ✅ Может ввести адрес заново если геокодирование неверное

### Статус
✅ **Исправлено** (15.12.2025 23:15)

---

## 🐛 Баг #7: Время звонка ошибочно помечается как ручное при установке времени прибытия

### Описание проблемы
При установке **только времени прибытия** (например, 12:50), рассчитанное время звонка (12:10) ошибочно помечалось как **ручное** (`is_manual=True`).

В результате в деталях заказа показывалось:
```
⏰ Время прибытия (ручное): 12:50
📞⏰ Время звонка (ручное): 12:10  ❌ НЕПРАВИЛЬНО!
```

Но время звонка должно показываться как **расчетное**, а не ручное!

### Причина
В `_update_manual_arrival_time` неправильно устанавливался флаг:

**Было (неправильно):**
```python
call_status.is_manual = True  # ❌ ВСЕГДА True!
```

Это означало что **любое** время звонка помечалось как ручное, даже если оно было рассчитано автоматически.

### Исправление
Флаг `is_manual` теперь зависит от того, было ли время звонка **действительно** установлено вручную:

**Файл:** `src/bot/handlers/order_handlers.py`

```python
# При обновлении существующей записи:
call_status.is_manual = call_time_was_manual  # ✅ True только если время ЗВОНКА было установлено вручную

# При создании новой записи:
new_call_status = CallStatusDB(
    call_time=calculated_call_time,
    arrival_time=manual_arrival_time,
    is_manual=False,  # ✅ Время звонка рассчитано автоматически
    ...
)
```

### Логика флага is_manual
- `is_manual=True` → Время **звонка** было установлено вручную через кнопку "📞⏰ Время звонка"
- `is_manual=False` → Время **звонка** рассчитано автоматически (даже если время прибытия установлено вручную)

### Теперь корректно:
```
⏰ Время прибытия (ручное): 12:50
📞⏰ Время звонка (ручное): Не указано  ✅
(Расчетное время звонка: 12:10)
```

### Статус
✅ **Исправлено** (16.12.2025 00:10)

---

## 🐛 Баг #2: Автоматический перерасчет времени звонка при установке времени прибытия

### Описание проблемы
После установки ручного времени прибытия, автоматически пересчитывается время звонка, даже если оно было установлено вручную ранее.

### Причина
Неправильный порядок операций в `_update_manual_arrival_time`:
- Обновляли `manual_arrival_time` в БД
- Затем загружали данные из БД
- Проверяли `order_data.get('manual_arrival_time')` (получали старое значение)

### Исправление
Изменен порядок операций:

**Файл:** `src/bot/handlers/order_handlers.py`

```python
def _update_manual_arrival_time(self, user_id, order_number, manual_arrival_time, message):
    # СНАЧАЛА загружаем данные заказа ДО обновления
    orders_data = self.parent.db_service.get_today_orders(user_id)
    order_data = None
    for od in orders_data:
        if od.get('order_number') == order_number:
            order_data = od
            break
    
    # ПОТОМ обновляем manual_arrival_time в БД
    self.parent.db_service.update_order(...)
    
    # Используем загруженные СТАРЫЕ данные для проверки
    # был ли уже установлен manual_arrival_time раньше
```

### Статус
✅ **Исправлено** (15.12.2025 22:30)

---

## ✅ Анализ использования полей времени

### КРИТИЧНЫЕ поля (влияют на логику):
1. ✅ `orders.manual_arrival_time` → Оптимизация маршрута (жесткое ограничение ±5 мин для OR-Tools)
2. ✅ `call_status.call_time` → Отправка уведомлений о звонках (фильтр `call_time <= now`)
3. ✅ `call_status.is_manual` → Защита от перезаписи при реоптимизации

### ИНФОРМАТИВНЫЕ поля (только отображение):
1. ✅ `call_status.arrival_time` → Только для показа в уведомлениях

---

## 📋 Чек-лист проверки после исправлений

- [ ] Переход между меню не вызывает ошибок
- [ ] Ручное время звонка сохраняется при установке времени прибытия
- [ ] Ручное время прибытия сохраняется при установке времени звонка
- [ ] Оптимизация маршрута учитывает ручные времена
- [ ] Уведомления о звонках отправляются по расписанию
- [ ] Реоптимизация не перезаписывает ручные времена

