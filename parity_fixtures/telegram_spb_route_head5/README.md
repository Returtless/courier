Subset of Telegram CourierPlanningBot route (first 5 stops). Matrix = haversine + constant speed; order/times differ from live 2GIS/Yandex.
Regenerate: `python tools/parity/generate_telegram_spb_fixtures.py` then `python tools/export_optimizer_golden.py --fixture parity_fixtures/telegram_spb_route_head5`.
