# 🔄 Database Migrations (Alembic)

Этот проект использует **Alembic** для автоматических миграций базы данных (аналог Flyway для Java).

## 📋 Как это работает

### Автоматическое применение миграций

Миграции **автоматически применяются** при каждом запуске приложения:

1. Контейнер стартует
2. `main.py` вызывает `migrate.py`
3. Alembic проверяет и применяет новые миграции
4. Приложение запускается

**Вам не нужно ничего делать вручную!** ✅

---

## 🛠️ Создание новых миграций

### Вариант 1: Автоматическая генерация (рекомендуется)

Alembic может автоматически обнаружить изменения в моделях:

```bash
# Локально или в контейнере
alembic revision --autogenerate -m "описание изменений"
```

Пример:
```bash
alembic revision --autogenerate -m "add status column to orders"
```

### Вариант 2: Пустая миграция (ручное редактирование)

```bash
alembic revision -m "описание изменений"
```

Затем отредактируйте созданный файл в `alembic/versions/`:

```python
def upgrade() -> None:
    op.add_column('orders', sa.Column('status', sa.String(), nullable=True))

def downgrade() -> None:
    op.drop_column('orders', 'status')
```

---

## 📁 Структура проекта

```
├── alembic/
│   ├── versions/          # Файлы миграций
│   │   └── 001_add_manual_time_fields.py
│   ├── env.py            # Конфигурация Alembic
│   └── script.py.mako    # Шаблон для новых миграций
├── alembic.ini           # Настройки Alembic
├── migrate.py            # Скрипт автоприменения миграций
└── requirements.txt      # alembic==1.13.1
```

---

## 🔍 Проверка статуса миграций

### Посмотреть текущую версию БД

```bash
python migrate.py status
```

Или напрямую:

```bash
alembic current
```

### Посмотреть историю миграций

```bash
alembic history
```

---

## 🚀 Применение миграций вручную

Обычно миграции применяются автоматически, но если нужно вручную:

```bash
# Применить все миграции
python migrate.py

# Или через alembic
alembic upgrade head
```

### Откатить последнюю миграцию

```bash
alembic downgrade -1
```

### Откатить до конкретной версии

```bash
alembic downgrade <revision_id>
```

---

## 📝 Пример создания новой миграции

### 1. Изменяем модель

```python
# src/models/order.py
class OrderDB(Base):
    # ... существующие поля ...
    priority = Column(Integer, default=0)  # новое поле
```

### 2. Генерируем миграцию

```bash
alembic revision --autogenerate -m "add priority to orders"
```

### 3. Проверяем сгенерированный файл

Alembic создаст файл в `alembic/versions/` с примерно таким содержимым:

```python
def upgrade() -> None:
    op.add_column('orders', sa.Column('priority', sa.Integer(), nullable=True, server_default='0'))

def downgrade() -> None:
    op.drop_column('orders', 'priority')
```

### 4. Коммитим и пушим

```bash
git add alembic/versions/*.py src/models/order.py
git commit -m "feat: add priority field to orders"
git push
```

### 5. Деплой

GitHub Actions пересоберет образ, Portainer webhook перезапустит контейнер, миграция применится **автоматически**! 🎉

---

## ⚠️ Важные правила

### ✅ DO (делайте)

- Всегда создавайте миграции для изменений схемы БД
- Проверяйте сгенерированные миграции перед коммитом
- Используйте осмысленные названия миграций
- Тестируйте миграции локально перед деплоем
- Коммитьте миграции вместе с изменениями моделей

### ❌ DON'T (не делайте)

- Не редактируйте существующие миграции (создавайте новые)
- Не удаляйте старые миграции (могут быть применены на других окружениях)
- Не изменяйте порядок миграций (revision chain)
- Не применяйте миграции вручную на production (используйте автоматическое применение)

---

## 🐛 Troubleshooting

### Миграция не применяется

1. Проверьте логи контейнера:
```bash
docker logs courier_bot
```

2. Проверьте DATABASE_URL:
```bash
docker exec courier_bot env | grep DATABASE
```

3. Проверьте таблицу версий:
```sql
SELECT * FROM alembic_version;
```

### "Target database is not up to date"

Означает, что в коде есть миграции новее, чем в БД. Они применятся при следующем запуске автоматически.

### Конфликт миграций

Если несколько веток создали миграции одновременно:

```bash
alembic merge <rev1> <rev2> -m "merge migrations"
```

---

## 📚 Полезные ссылки

- [Alembic Documentation](https://alembic.sqlalchemy.org/)
- [SQLAlchemy Documentation](https://docs.sqlalchemy.org/)
- [Alembic Tutorial](https://alembic.sqlalchemy.org/en/latest/tutorial.html)

---

## 💡 FAQ

**Q: Нужно ли запускать миграции вручную после деплоя?**  
A: Нет! Они применяются автоматически при старте контейнера.

**Q: Что если миграция упадет с ошибкой?**  
A: Приложение не запустится, в логах будет ошибка. Исправьте миграцию и пересоберите образ.

**Q: Можно ли откатить миграцию на production?**  
A: Технически да (`alembic downgrade`), но лучше создать новую миграцию для исправления.

**Q: Как протестировать миграцию локально?**  
A: Запустите `python migrate.py` с локальной БД перед коммитом.

