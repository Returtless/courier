# 🚚 Courier Route Optimization Bot

Telegram бот для оптимизации маршрутов доставки с учетом пробок в реальном времени, временных интервалов доставки и управления заказами.

## ✨ Возможности

- 📍 **Оптимизация маршрутов** с учетом реального трафика через 2GIS API
- ⏰ **Управление временными окнами** доставки
- 🕐 **Ручное управление временем** - установка точного времени звонка и прибытия для любого заказа с защитой от перезаписи при реоптимизации
- 📞 **Автоматические уведомления** о звонках с кнопками подтверждения/отклонения
- 🔄 **Мониторинг пробок** в реальном времени
- ⚙️ **Персональные настройки** - каждый пользователь может настроить параметры работы под себя
- 💾 **База данных** с привязкой к пользователю и дате
- 🗑️ **Сброс данных** за текущий день
- ✅ **Отметка доставленных** заказов
- 🗺️ **Кликабельные ссылки** на маршруты и точки в 2GIS и Яндекс Картах
- 📱 **Удобный интерфейс** с кнопками для всех действий
- 🔍 **Быстрый поиск** заказов по номеру
- 📋 **Информативные кнопки** заказов с полной информацией
- 💬 **Система звонков** с повторными попытками (настраиваемое количество)
- 🗄️ **Кэширование** геокодирования и маршрутов для минимизации API запросов
- 📊 **Структурированное хранение** данных маршрутов и звонков
- 🏢 **Поддержка подъездов и квартир** для точного построения маршрутов
- 👥 **Многопользовательский режим** - несколько курьеров могут работать одновременно, каждый видит только свои заказы и маршруты
- 📲 **Автоматический импорт заказов** - импорт из веб-приложения ШефМаркет через встроенный браузер (Playwright)
- 🔐 **Безопасное хранение** учетных данных с шифрованием (Fernet)
- 🧪 **46 unit-тестов** - автоматическое тестирование критических компонентов

## 🛠️ Технологии

- **Python 3.11+**
- **pyTelegramBotAPI** - Telegram Bot API
- **SQLAlchemy** - ORM для базы данных
- **Alembic** - автоматические миграции базы данных
- **2GIS API** - карты и маршруты с пробками (основной)
- **Yandex Maps API** - резервный вариант для геокодирования
- **Geopy** - fallback для геокодирования
- **OR-Tools** - оптимизация маршрутов (VRP/TSP)
- **PostgreSQL/SQLite** - база данных
- **Playwright** - автоматизация браузера для импорта заказов из веб-приложений
- **Cryptography (Fernet)** - шифрование учетных данных пользователей

## 📦 Установка

### Локальная установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd courier
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
```bash
cp env.example env
```

Отредактируйте файл `env`:
```env
# Telegram Bot Token (получите у @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# 2GIS API Key (получите на dev.2gis.com)
TWO_GIS_API_KEY=your_2gis_api_key_here

# Yandex Maps API Key (опционально, для резервного геокодирования)
YANDEX_MAPS_API_KEY=your_yandex_maps_key_here

# База данных
# SQLite (для разработки):
DATABASE_URL=sqlite:///./courier_bot.db
# PostgreSQL (для продакшена):
# DATABASE_URL=postgresql://courier:courier_password@localhost:5432/courier_db

# Ключ шифрования для учетных данных (сгенерируется автоматически если не указан)
ENCRYPTION_KEY=your_generated_fernet_key_here
```

**Генерация ENCRYPTION_KEY:**
```python
from cryptography.fernet import Fernet
print(Fernet.generate_key().decode())
```

5. **Запустите бота:**
```bash
python main.py
```

### Docker установка

#### Вариант 1: Использование готового образа из GitHub Container Registry

1. **Создайте файл `docker-compose.prod.yml`:**
```yaml
services:
  bot:
    image: ghcr.io/YOUR_USERNAME/courier:latest
    container_name: courier_bot
    restart: unless-stopped
    environment:
      - TZ=Europe/Moscow
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - YANDEX_MAPS_API_KEY=${YANDEX_MAPS_API_KEY}
      - TWO_GIS_API_KEY=${TWO_GIS_API_KEY}
      - DATABASE_URL=${DATABASE_URL}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - LLM_MODEL_PATH=${LLM_MODEL_PATH:-models/gemma3-4b}
      - LLM_DEVICE=${LLM_DEVICE:-cpu}
    volumes:
      - ./data:/app/data
    depends_on:
      - postgres
    networks:
      - courier_network

  postgres:
    image: postgres:15-alpine
    container_name: courier_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-courier}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-courier_password}
      POSTGRES_DB: ${POSTGRES_DB:-courier_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - courier_network
    ports:
      - "5432:5432"

volumes:
  postgres_data:

networks:
  courier_network:
    driver: bridge
```

2. **Войдите в GitHub Container Registry:**
```bash
echo $GITHUB_TOKEN | docker login ghcr.io -u YOUR_USERNAME --password-stdin
# Или используйте Personal Access Token с правами read:packages
```

3. **Настройте переменные окружения в Portainer:**
   - Перейдите в раздел "Environment variables" вашего стека в Portainer
   - Добавьте все необходимые переменные (см. раздел "Переменные окружения" ниже)
   - Убедитесь, что `DATABASE_URL` указывает на PostgreSQL: `postgresql://courier:courier_password@postgres:5432/courier_db`

4. **Запустите через Docker Compose:**
```bash
docker-compose -f docker-compose.prod.yml up -d
```

#### Вариант 2: Локальная сборка образа (через Portainer)

1. **Настройте переменные окружения в Portainer:**
   - Перейдите в раздел "Environment variables" вашего стека в Portainer
   - Добавьте все необходимые переменные (см. раздел "Переменные окружения" ниже)
   - Убедитесь, что `DATABASE_URL` указывает на PostgreSQL: `postgresql://courier:courier_password@postgres:5432/courier_db`

2. **Запустите стек через Portainer:**
   - Загрузите `docker-compose.yml` в Portainer
   - Portainer автоматически использует переменные окружения из раздела "Environment variables"

3. **Проверьте логи:**
```bash
docker-compose logs -f bot
# Или через Portainer: перейдите в контейнер → Logs
```

## 📱 Использование

### Главное меню

Бот предоставляет удобное меню с кнопками, сгруппированными по категориям:

#### 📦 Заказы:
- **➕ Добавить заказы** - добавить заказы для доставки
- **ℹ️ Детали заказа** - просмотр и редактирование заказов (с информативными кнопками)
- **✅ Доставленные** - список доставленных заказов
- **🔍 Найти по номеру** - быстрый поиск заказа по номеру

#### 🗺️ Маршрут:
- **📋 Показать маршрут** - просмотреть текущий маршрут
- **📍 Точка старта** - установить точку и время начала маршрута
- **▶️ Оптимизировать** - построить оптимальный маршрут
- **📞 Звонки** - график звонков клиентам
- **🚦 Мониторинг** - запустить мониторинг пробок
- **🛑 Стоп мониторинг** - остановить мониторинг

#### Другие:
- **⚙️ Настройки** - персональные настройки пользователя
- **🗑️ Сбросить день** - удалить все данные за сегодня

### 📲 Автоматический импорт заказов из ШефМаркет

Бот может автоматически импортировать заказы из веб-приложения ШефМаркет, избавляя от необходимости делать множество скриншотов на смартфоне.

#### Первоначальная настройка:

1. **Сохраните учетные данные** (делается один раз):
```
/set_credentials ваш_логин ваш_пароль
```
Пример:
```
/set_credentials ivan@mail.ru mypassword123
```

🔒 **Безопасность:**
- Логин и пароль шифруются с помощью Fernet (симметричное шифрование)
- Хранятся в базе данных в зашифрованном виде
- Никогда не передаются никуда, кроме официального сайта ШефМаркет
- Сообщение с паролем автоматически удаляется после сохранения

2. **Импортируйте заказы** (выполняется каждый день):
```
/import_orders
```

Бот автоматически:
- 🌐 Откроет браузер (Chromium) на сервере
- 🔐 Авторизуется на сайте ШефМаркет
- 📋 Получит список заказов на сегодня
- 📄 Перейдет на страницу каждого заказа
- 📦 Извлечет все данные:
  - Номер заказа
  - Имя клиента
  - Телефон
  - Полный адрес (включая подъезд, квартиру, этаж, домофон)
  - Временное окно доставки
  - Комментарии (включая "Бесконтактная доставка")
- 💾 Сохранит все в базу данных
- 🗺️ Подготовит заказы для оптимизации маршрута

**После импорта** сразу используйте **▶️ Оптимизировать** для построения маршрута!

#### Управление учетными данными:

- **Обновить учетные данные:**
```
/set_credentials новый_логин новый_пароль
```

- **Удалить учетные данные:**
```
/delete_credentials
```

#### Примечания:
- Импорт выполняется в headless режиме (без GUI) на сервере
- Весь процесс занимает ~30-60 секунд в зависимости от количества заказов
- Бот автоматически пропускает уже импортированные заказы (по номеру заказа)
- Если структура сайта ШефМаркет изменится, селекторы могут потребовать обновления

### ⚙️ Настройки

Каждый пользователь может настроить параметры работы бота под свои нужды. Доступ к настройкам - через кнопку **⚙️ Настройки** в главном меню.

#### Доступные настройки:

##### 📞 Звонки:
- **Время звонка до приезда** (1-60 минут, по умолчанию: 10) - за сколько минут до прибытия на точку звонить клиенту
- **Интервал повторных звонков** (1-15 минут, по умолчанию: 2) - через сколько минут повторить звонок после отклонения
- **Максимум попыток дозвона** (1-10 раз, по умолчанию: 3) - сколько раз пытаться дозвониться до клиента

##### ⏱️ Время:
- **Время на точке** (1-60 минут, по умолчанию: 10) - сколько времени тратится на доставку одного заказа
- **Время на парковку** (0-30 минут, по умолчанию: 7) - время на парковку и подход к подъезду

##### 🚦 Мониторинг пробок:
- **Интервал проверки** (1-60 минут, по умолчанию: 5) - как часто проверять изменения в пробках
- **Порог уведомлений** (10-200%, по умолчанию: 50%) - при каком увеличении времени в пути отправлять уведомление

Все настройки можно сбросить к значениям по умолчанию кнопкой **🔄 Сбросить к умолчанию**.

### Форматы добавления заказов

#### 1️⃣ Формат с временным окном (рекомендуемый):
```
Время НомерЗаказа Адрес
```

Примеры:
```
10:00 - 13:00 3258104 г Санкт-Петербург, ул Манчестерская, д 3 стр 1
10:00 - 13:00 3258981 г Санкт-Петербург, ул Манчестерская, д 3 к 2 стр 1
10:00 - 13:00 3259615 г Санкт-Петербург, Фермское шоссе, д 14 к 1
```

Можно добавить несколько заказов одним сообщением (каждый на новой строке).

#### 2️⃣ Расширенный формат:
```
Имя|Телефон|Адрес|Комментарий
```

Примеры:
```
Иван Иванов|+7-999-123-45-67|ул. Ленина, д.10, кв.5|Звонок в домофон 05
Анна Петрова|+7-999-987-65-43|пр. Победы, д.25|Оставить у двери
```

После добавления всех заказов нажмите кнопку **✅ Готово**.

### Редактирование заказов

1. Нажмите **ℹ️ Детали заказа**
2. Выберите заказ из списка кнопок (каждая кнопка содержит полную информацию о заказе)
   - Или введите номер заказа прямо в чат для быстрого доступа
3. Используйте кнопки для редактирования:
   - **📞 Телефон** - изменить номер телефона
   - **👤 ФИО** - изменить имя клиента
   - **💬 Комментарий** - изменить комментарий
   - **🏢 Подъезд** - добавить/изменить номер подъезда
   - **🚪 Квартира** - добавить/изменить номер квартиры
   - **🕐 Время доставки** - изменить временное окно доставки
   - **⏰ Время прибытия** - установить точное время прибытия (вручную)
   - **📞⏰ Время звонка** - установить точное время звонка (вручную)
   - **✅ Доставлен** - пометить заказ как доставленный

**Особенности:**
- Кнопки заказов показывают всю заполненную информацию (номер, время, имя, телефон, адрес, подъезд, квартира)
- Сразу видно, какие поля заполнены, а какие нет (❌ означает незаполненное поле)
- Подъезд автоматически добавляется к адресу для точного построения маршрута
- Можно ввести номер заказа в чат в любое время для быстрого доступа

### Ручное управление временем (NEW!)

Для любого заказа можно установить точное время прибытия или звонка вручную:

#### Установка времени звонка:
1. Выберите заказ → **📞⏰ Время звонка**
2. Введите время в формате `ЧЧ:ММ` (например, `12:50`)
3. Бот автоматически рассчитает время прибытия (время звонка + 40 минут)

#### Установка времени прибытия:
1. Выберите заказ → **⏰ Время прибытия**
2. Введите время в формате `ЧЧ:ММ` (например, `13:40`)
3. Бот автоматически рассчитает время звонка (время прибытия - 40 минут)

**Защита от перезаписи:**
- ✅ Ручно установленные времена **НЕ изменяются** при реоптимизации маршрута
- ✅ При добавлении новых заказов и реоптимизации, ручные времена остаются фиксированными
- ✅ Алгоритм оптимизации учитывает ручные времена как жесткие ограничения
- ✅ Маршрут строится с учетом фиксированных точек

**Независимые времена:**
- Можно установить только время звонка (прибытие рассчитается автоматически)
- Можно установить только время прибытия (звонок рассчитается автоматически)
- Можно установить оба времени независимо (например: звонок в 13:00, прибытие в 13:00)

### Точка старта

1. Нажмите **📍 Точка старта** в меню "Маршрут"
2. Выберите способ:
   - **📍 Геопозиция** - отправить текущее местоположение
   - **✍️ Адрес** - ввести адрес вручную
3. При вводе адреса бот автоматически определит координаты и покажет ссылку на карту для подтверждения
4. Укажите время начала маршрута в формате `ЧЧ:ММ` (например, `09:00`)
5. Можно изменить время старта через кнопку **⏰ Время старта**

### Оптимизация маршрута

1. Добавьте заказы
2. Установите точку старта
3. Нажмите **▶️ Оптимизировать** в меню "Маршрут"

**Процесс оптимизации:**
- Бот показывает прогресс в реальном времени
- Геокодирует все адреса (использует кэш для уже известных адресов)
- Определяет координаты точки старта (если еще не определены)
- Строит оптимальный маршрут с учетом пробок через 2GIS API
- Учитывает временные окна доставки
- Рассчитывает время звонков (минимум 40 минут до доставки)
- Сохраняет координаты всех точек для последующих оптимизаций
- Создает записи о звонках для автоматических уведомлений

**Результат:**
- Оптимальный порядок доставки заказов
- Кликабельные ссылки на маршруты и точки
- Полный график звонков

### Просмотр маршрута

Нажмите **📋 Показать маршрут** для просмотра:
- Порядок доставки заказов
- Время прибытия к каждому клиенту
- Расстояние и время в пути между точками
- Ссылки на маршруты в 2GIS и Яндекс Картах
- Ссылки на каждую точку на карте
- Статус доставки (в окне/раньше/позже)
- Время звонка каждому клиенту
- Полная информация о клиенте (имя, телефон, комментарий, подъезд, квартира)
- Статус звонка (✅ подтвержден, ❌ отклонен, 🔴 недозвон)

### График звонков и уведомления

Нажмите **📞 Звонки** для просмотра:
- Время звонка каждому клиенту
- Номер телефона (кликабельный в Telegram)
- Время доставки

**Автоматические уведомления:**
- Бот автоматически отправляет уведомление, когда наступает время звонка
- В уведомлении есть кнопки:
  - **✅ Подтверждено** - звонок совершен, можно добавить комментарий
  - **❌ Отклонено** - повтор через 2 минуты (максимум 3 попытки)
- После 3 отклонений заказ помечается как "НЕДОЗВОН" в маршруте
- Телефон в сообщении автоматически кликабельный в Telegram

### Сброс данных за день

Нажмите **🗑️ Сбросить день** для удаления:
- Всех заказов за сегодня
- Точки старта
- Маршрута и графика звонков

**Внимание:** Это действие нельзя отменить!

## 🗺️ Работа с картами

- **2GIS API** - основной сервис для построения маршрутов с пробками
- **Yandex Maps API** - резервный вариант для геокодирования
- **Geopy** - fallback для геокодирования (OpenStreetMap)
- **Реальное время** пробок через 2GIS
- **Кликабельные ссылки** на маршруты и точки в 2GIS и Яндекс Картах

## 📊 Оптимизация маршрутов

- **OR-Tools** - продвинутая оптимизация от Google
- **VRP (Vehicle Routing Problem)** - оптимизация для нескольких курьеров
- **TSP (Traveling Salesman Problem)** - оптимизация для одного курьера
- **Time Windows** - учет временных интервалов доставки
- **Traffic-aware** - учет пробок в реальном времени
- **Constraint optimization** - ограничения по времени и расстоянию
- **10 минут на доставку** - учитывается время парковки и подхода к клиенту

## 💾 База данных

Все данные хранятся в базе данных с привязкой к:
- **Пользователю** (Telegram user ID)
- **Дате** (заказы привязаны к конкретному дню)

### Структура БД:

- **orders** - заказы с полной информацией (номер, адрес, телефон, имя, комментарий, подъезд, квартира, время доставки, координаты, `manual_arrival_time`)
- **start_locations** - точки старта по датам (адрес, координаты, время старта)
- **route_data** - сохраненные маршруты и графики звонков (структурированные данные)
- **call_status** - статусы звонков (время, попытки, статус, комментарии, `is_manual`, `arrival_time`)
- **geocode_cache** - кэш геокодирования для минимизации API запросов
- **user_settings** - персональные настройки пользователей
- **credentials** - зашифрованные учетные данные для импорта

### Миграции БД (Alembic):

Проект использует Alembic для автоматических миграций базы данных:

```bash
# Применить все миграции
alembic upgrade head

# Откатить последнюю миграцию
alembic downgrade -1

# Посмотреть текущую версию
alembic current

# История миграций
alembic history
```

**Текущие миграции:**
- `001_initial` - начальная структура БД
- `002_add_user_settings` - добавление таблицы настроек пользователей
- `003_refactor_manual_times` - рефакторинг ручного времени (добавление `is_manual` и `arrival_time` в `call_status`, удаление `manual_call_time` из `orders`)

Миграции применяются автоматически при запуске бота.

### Поддерживаемые БД:

- **SQLite** - для разработки (по умолчанию)
- **PostgreSQL** - для продакшена (через Docker)

## 🔧 Настройка

### Параметры в `src/config.py`:

- `delivery_time_per_stop`: время на доставку одного заказа (10 мин)
- `parking_walking_time`: время на парковку и подход (7 мин)
- `call_advance_time`: минимум времени до звонка (40 мин)
- `traffic_check_interval`: интервал проверки пробок (5 мин)

## 🚦 Мониторинг пробок

- **Автоматическая проверка** каждые 5 минут
- **Уведомления об изменениях** в Telegram
- **Пересчет маршрутов** при значительных задержках
- **Пороги срабатывания** (50% увеличение времени)
- **Фоновая работа** без остановки бота

## 🐳 Docker

### Запуск через Docker Compose:

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Просмотр логов
docker-compose logs -f bot

# Перезапуск
docker-compose restart bot
```

### Переменные окружения для Docker (Portainer):

При использовании Docker через Portainer все переменные окружения настраиваются в разделе **"Environment variables"** стека в Portainer.

**Обязательные переменные:**
```
TELEGRAM_BOT_TOKEN=your_bot_token_here
TWO_GIS_API_KEY=your_2gis_api_key_here
DATABASE_URL=postgresql://courier:courier_password@postgres:5432/courier_db
ENCRYPTION_KEY=your_generated_fernet_key_here
```

**Опциональные переменные:**
```
YANDEX_MAPS_API_KEY=your_yandex_maps_key_here  # Резервный вариант для геокодирования
LLM_MODEL_PATH=models/gemma3-4b
LLM_DEVICE=cpu
```

**Генерация ENCRYPTION_KEY:**
```python
from cryptography.fernet import Fernet
print(Fernet.generate_key().decode())
```

**Важно:**
- Все переменные должны быть указаны в разделе "Environment variables" стека в Portainer
- После добавления/изменения переменных перезапустите стек
- `ENCRYPTION_KEY` критически важен для расшифровки сохраненных паролей - не меняйте его после первого использования!

PostgreSQL автоматически создастся при первом запуске.

## 📈 Мониторинг и логи

- **Логирование**: используется стандартный Python `logging` модуль
- **Уровни логирования**: DEBUG, INFO, WARNING, ERROR
- **Настройка уровня логирования**: измените `logging.basicConfig(level=logging.INFO)` в `main.py`
- **База данных**: `courier_bot.db` (SQLite) или PostgreSQL
- **Мониторинг маршрутов** в реальном времени
- **Кэширование**: геокодирование и маршруты кэшируются для минимизации API запросов
- Все данные сохраняются в БД с привязкой к пользователю и дате

### Структура логов:

- `logger.debug()` - отладочная информация (геокодирование, кэш)
- `logger.info()` - важные события (запуск бота, создание звонков)
- `logger.warning()` - предупреждения (ошибки API, кэш)
- `logger.error()` - критические ошибки (с полным стеком через `exc_info=True`)

## 🧪 Тестирование

### Unit-тесты (46 тестов)

Проект покрыт автоматическими unit-тестами для критических компонентов:

```bash
# Установка зависимостей для тестов
pip install -r requirements-dev.txt

# Запуск всех unit-тестов
pytest tests/unit/ -v --ignore=tests/unit/test_route_optimizer.py

# Запуск конкретного модуля
pytest tests/unit/test_models.py -v
pytest tests/unit/test_manual_times.py -v  # Критично после рефакторинга!
pytest tests/unit/test_call_notifier.py -v
pytest tests/unit/test_db_service.py -v
pytest tests/unit/test_settings_service.py -v

# С покрытием кода
pytest tests/unit/ --cov=src --cov-report=html
```

**Покрытие тестами:**
- ✅ **test_models.py** (13 тестов) - модели данных Order, CallStatusDB
- ✅ **test_manual_times.py** (18 тестов) - логика ручного времени (критично!)
- ✅ **test_call_notifier.py** (3 теста) - уведомления о звонках
- ✅ **test_db_service.py** (7 тестов) - работа с базой данных
- ✅ **test_settings_service.py** (5 тестов) - настройки пользователей

**Документация по тестированию:**
- `TESTING_STRATEGY.md` - полная стратегия тестирования
- `TESTING_QUICKSTART.md` - быстрый старт с тестами
- `MANUAL_TESTING_CHECKLIST.md` - чек-лист ручного тестирования (53 проверки)
- `tests/e2e/README.md` - руководство по E2E тестам

### Legacy тесты

Старые тестовые скрипты для ручной проверки API:

```bash
# Тест карт и геокодирования
python test_maps.py

# Тест 2GIS API (основной сервис)
python test_2gis_api.py

# Тест Yandex API (резервный)
python test_yandex_api.py

# Тест оптимизации маршрутов
python test_optimization.py

# Тест парсинга заказов
python test_parsing.py
```

**Примечание:** 
- Для запуска legacy тестов необходимо настроить API ключи (через переменные окружения или файл `env` для локальной разработки)
- Legacy тесты используют `print()` для вывода результатов
- Unit-тесты используют `pytest` и не требуют API ключей

## 🚀 CI/CD

Проект использует GitHub Actions для автоматической сборки и публикации Docker-образа в GitHub Container Registry.

### Автоматическая сборка

Workflow автоматически запускается при:
- Push в ветки `main` или `master`
- Создании тегов версий (например, `v1.0.0`)
- Ручном запуске через `workflow_dispatch`

### Использование образа

После успешной сборки образ будет доступен по адресу:
```
ghcr.io/YOUR_USERNAME/courier:latest
ghcr.io/YOUR_USERNAME/courier:v1.0.0  # для версионированных тегов
```

**Важно:** Замените `YOUR_USERNAME` на ваш GitHub username или название организации.

### Настройка доступа к образу

1. Перейдите в настройки репозитория → Packages
2. Выберите созданный пакет `courier`
3. Настройте видимость (public/private)
4. Для использования на сервере создайте Personal Access Token с правами `read:packages`

### Использование образа на сервере

Создайте файл `docker-compose.prod.yml`:
```yaml
services:
  bot:
    image: ghcr.io/YOUR_USERNAME/courier:latest
    container_name: courier_bot
    restart: unless-stopped
    environment:
      - TZ=Europe/Moscow
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - YANDEX_MAPS_API_KEY=${YANDEX_MAPS_API_KEY}
      - TWO_GIS_API_KEY=${TWO_GIS_API_KEY}
      - DATABASE_URL=${DATABASE_URL}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - LLM_MODEL_PATH=${LLM_MODEL_PATH:-models/gemma3-4b}
      - LLM_DEVICE=${LLM_DEVICE:-cpu}
    volumes:
      - ./data:/app/data
    depends_on:
      - postgres
    networks:
      - courier_network

  postgres:
    image: postgres:15-alpine
    container_name: courier_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-courier}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-courier_password}
      POSTGRES_DB: ${POSTGRES_DB:-courier_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - courier_network
    ports:
      - "5432:5432"

volumes:
  postgres_data:

networks:
  courier_network:
    driver: bridge
```

**Настройте переменные окружения в Portainer:**
- Перейдите в раздел "Environment variables" стека
- Добавьте все необходимые переменные (см. раздел "Переменные окружения для Docker" выше)

Войдите в GitHub Container Registry:
```bash
echo $GITHUB_TOKEN | docker login ghcr.io -u YOUR_USERNAME --password-stdin
```

Запустите:
```bash
docker-compose -f docker-compose.prod.yml up -d
```

## 🗺️ Roadmap

### ✅ Реализовано:
- [x] OR-Tools оптимизация маршрутов
- [x] Real-time мониторинг пробок
- [x] База данных с привязкой к пользователю и дате
- [x] Docker поддержка
- [x] 2GIS API интеграция
- [x] Кликабельные ссылки на маршруты и точки
- [x] Управление заказами через кнопки
- [x] Отметка доставленных заказов
- [x] Автоматические уведомления о звонках
- [x] Система повторных попыток звонков
- [x] Информативные кнопки заказов с полной информацией
- [x] Быстрый поиск заказов по номеру
- [x] Кэширование геокодирования и маршрутов
- [x] Структурированное хранение данных маршрутов
- [x] Логирование через стандартный logging модуль
- [x] Поддержка подъездов и квартир
- [x] Обновление маршрута при изменении заказа
- [x] **Ручное управление временем прибытия и звонка** (NEW!)
- [x] **Защита от перезаписи при реоптимизации** (NEW!)
- [x] **46 unit-тестов** для критических компонентов (NEW!)
- [x] **Автоматические миграции БД** через Alembic (NEW!)
- [x] **Автоматический импорт из ШефМаркет** через Playwright (NEW!)
- [x] **Многопользовательский режим** с изоляцией данных (NEW!)

### 🔄 В планах:
- [ ] Integration-тесты с реальными API
- [ ] E2E тесты полных сценариев
- [ ] Интеграция с Google Maps API (резерв)
- [ ] Мобильное приложение для курьеров
- [ ] Веб-интерфейс для диспетчеров
- [ ] Аналитика эффективности маршрутов
- [ ] Интеграция с другими системами учета заказов
- [ ] LLM интеграция (Gemma3-4B) для анализа комментариев

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. **Проверьте логи** - используйте `logging` для отладки:
   ```python
   import logging
   logging.basicConfig(level=logging.DEBUG)
   ```

2. **API ключи** - убедитесь, что все ключи настроены в Portainer (раздел "Environment variables"):
   - `TELEGRAM_BOT_TOKEN` - обязателен
   - `TWO_GIS_API_KEY` - обязателен для маршрутов
   - `YANDEX_MAPS_API_KEY` - опционален (резерв)
   - `ENCRYPTION_KEY` - обязателен для шифрования паролей
   - `DATABASE_URL` - обязателен для подключения к PostgreSQL

3. **Версия Python** - требуется 3.11+

4. **База данных** - проверьте доступность:
   - SQLite: файл `courier_bot.db` должен быть доступен для записи
   - PostgreSQL: проверьте подключение и права доступа

5. **Docker** - если используете Docker:
   ```bash
   docker-compose logs -f bot  # Просмотр логов
   docker-compose restart bot  # Перезапуск
   ```

6. **Часовой пояс** - бот использует `Europe/Moscow`, убедитесь, что Docker контейнер настроен правильно

## 🔑 Получение API ключей

### 2GIS API:
1. Зарегистрируйтесь на [dev.2gis.com](https://dev.2gis.com)
2. Создайте приложение
3. Получите API ключ для Routing API

### Yandex Maps API (опционально):
1. Зарегистрируйтесь на [developer.tech.yandex.ru](https://developer.tech.yandex.ru)
2. Создайте ключ для JavaScript API и HTTP Геокодер

### Telegram Bot Token:
1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Используйте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен
