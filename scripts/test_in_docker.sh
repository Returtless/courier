#!/bin/bash
# Скрипт для запуска тестов в Docker контейнере

echo "🐳 Запуск тестов в Docker контейнере..."

# Проверяем, запущен ли контейнер
if ! docker ps | grep -q courier_bot; then
    echo "❌ Контейнер courier_bot не запущен"
    echo "💡 Запустите сначала: docker-compose up -d"
    exit 1
fi

# Запускаем тесты в контейнере
echo "📋 Запуск тестов..."
docker exec courier_bot python scripts/test_bot_functions.py

echo ""
echo "✅ Тесты завершены"

