# 🔄 Рефакторинг ручного времени прибытия и звонка

## 📋 Описание изменений

Проведен комплексный рефакторинг логики работы с ручным временем прибытия и звонка для исключения дублирования данных и обеспечения корректной работы при реоптимизации маршрута.

---

## ✅ Что было сделано

### 1️⃣ Обновлена схема БД

#### Таблица `call_status`
**Добавлены поля:**
- `arrival_time` (DateTime, nullable) - время прибытия (для ручных установок)
- `is_manual` (Boolean, default=False) - флаг ручной установки времени

#### Таблица `orders`
**Удалено поле:**
- `manual_call_time` - удалено дублирование (теперь хранится только в `call_status`)

**Сохранено поле:**
- `manual_arrival_time` - используется как ограничение для алгоритма оптимизации

#### Миграция
Создана миграция `003_refactor_manual_times.py` для автоматического обновления схемы БД при деплое.

---

### 2️⃣ Логика установки ручного времени

#### Установка времени звонка
**Файл:** `src/bot/handlers/order_handlers.py`
**Метод:** `_update_manual_call_time`

**Логика:**
```python
# Пользователь вводит: "12:50"
call_time = 12:50
arrival_time = 12:50 + 40 мин (advance_time) = 13:30

→ call_status(
    call_time = 12:50,
    arrival_time = 13:30,  # рассчитано
    is_manual = True
)
```

#### Установка времени прибытия
**Файл:** `src/bot/handlers/order_handlers.py`
**Метод:** `_update_manual_arrival_time`

**Логика:**
```python
# Пользователь вводит: "13:40"
arrival_time = 13:40
call_time = 13:40 - 40 мин (advance_time) = 13:00

→ orders.manual_arrival_time = 13:40  # ограничение для оптимизации
→ call_status(
    call_time = 13:00,  # рассчитано
    arrival_time = 13:40,
    is_manual = True
)
```

#### Независимые времена
Пользователь может установить:
- Только время звонка (arrival_time рассчитывается)
- Только время прибытия (call_time рассчитывается)
- Оба времени независимо (например: звонок 13:00, прибытие 13:00)

---

### 3️⃣ Оптимизация маршрута с учетом ручного времени

#### Алгоритм приоритетов
**Файл:** `src/services/route_optimizer.py`
**Метод:** `_solve_vrp`

**Приоритет временных ограничений:**

1. **Приоритет 1:** `orders.manual_arrival_time` (если установлено)
   - Фиксированное время прибытия ±5 минут для возможности решения
   - Алгоритм VRP строит маршрут с учетом этого жесткого ограничения

2. **Приоритет 2:** `delivery_time_window` (если установлено и нет ручного времени)
   - Временное окно доставки из заказа

3. **Приоритет 3:** Автоматический расчет (если ничего не установлено)
   - Алгоритм сам определяет оптимальное время

**Код:**
```python
# Добавлены в _solve_vrp():
if order.manual_arrival_time:
    # Фиксированное время прибытия
    time_diff = (order.manual_arrival_time - start_time).total_seconds()
    arrival_seconds_min = max(0, int(time_diff - 5*60))  # -5 мин
    arrival_seconds_max = int(time_diff + 5*60)          # +5 мин
    time_dimension.CumulVar(node_index).SetRange(arrival_seconds_min, arrival_seconds_max)
elif order.delivery_time_start and order.delivery_time_end:
    # Стандартное временное окно
    ...
```

---

### 4️⃣ Защита от перезаписи ручных установок

#### Обновлен метод `create_call_status`
**Файл:** `src/services/call_notifier.py`

**Изменения:**
- Добавлены параметры `is_manual` и `arrival_time`
- Добавлена проверка: **не перезаписывать** `call_status` с `is_manual=True` при автоматической оптимизации

**Код:**
```python
if existing:
    # ВАЖНО: Не перезаписываем ручные установки!
    if existing.is_manual and not is_manual:
        logger.info(f"⏭️ Пропускаем обновление - установлено вручную")
        return existing
    
    # Обновляем только автоматические
    existing.call_time = call_time
    existing.arrival_time = arrival_time
    existing.is_manual = is_manual
    ...
```

**Обновлены вызовы:**
- `route_handlers.py` - при оптимизации маршрута: `is_manual=False`
- `order_handlers.py` - при обновлении телефона: `is_manual=False`

---

### 5️⃣ Отображение ручного времени

#### Обновлен метод `show_order_details`
**Файл:** `src/bot/handlers/order_handlers.py`

**Логика отображения:**

**Время прибытия:**
1. Приоритет: `orders.manual_arrival_time`
2. Резерв: `call_status.arrival_time` (если `is_manual=True`)
3. Иначе: "Не указано"

**Время звонка:**
1. `call_status.call_time` (если `is_manual=True`)
2. Иначе: "Не указано"

**Код:**
```python
# Загружаем call_status
with get_db_session() as session:
    call_status = session.query(CallStatusDB).filter(...).first()
    
    if call_status and call_status.is_manual:
        manual_call_time_display = call_status.call_time.strftime('%H:%M')
        manual_arrival_time_display = call_status.arrival_time.strftime('%H:%M')

# Отображаем с приоритетом orders.manual_arrival_time
if order_data.get('manual_arrival_time'):
    details.append(f"⏰ Время прибытия (ручное): {manual_arrival_time}")
elif manual_arrival_time_display:
    details.append(f"⏰ Время прибытия (ручное): {manual_arrival_time_display}")
else:
    details.append(f"⏰ Время прибытия (ручное): Не указано")
```

---

## 📊 Архитектура данных

### Хранение времен

| Поле | Таблица | Назначение | Когда установлено |
|------|---------|------------|-------------------|
| `manual_arrival_time` | `orders` | Ограничение для оптимизации | Пользователь установил время прибытия |
| `call_time` | `call_status` | Время звонка (ручное/авто) | Всегда при наличии маршрута |
| `arrival_time` | `call_status` | Время прибытия (для справки) | Всегда при наличии маршрута |
| `is_manual` | `call_status` | Флаг ручной установки | `True` если пользователь установил вручную |

### Приоритеты при оптимизации

```
1. orders.manual_arrival_time  → Фиксированное ограничение для OR-Tools
2. delivery_time_window        → Гибкое временное окно
3. Автоматический расчет       → Нет ограничений
```

### Защита при реоптимизации

```python
# При повторной оптимизации маршрута:
if existing_call_status.is_manual:
    # НЕ перезаписывать! Пользователь установил вручную
    pass
else:
    # Обновить автоматическое время
    existing_call_status.call_time = new_call_time
    existing_call_status.arrival_time = new_arrival_time
```

---

## 🚀 Что это дает

### ✅ Преимущества:

1. **Нет дублирования данных**
   - Время звонка хранится только в `call_status`
   - Единый источник правды

2. **Защита от перезаписи**
   - Ручные установки не теряются при реоптимизации
   - Автоматические времена обновляются корректно

3. **Гибкость**
   - Можно установить только время звонка
   - Можно установить только время прибытия
   - Можно установить оба независимо

4. **Корректная оптимизация**
   - Алгоритм учитывает ручные времена как жесткие ограничения
   - Строит маршрут с учетом фиксированных точек

5. **Логичное отображение**
   - Пользователь видит установленные вручную времена
   - Понятные подсказки "ручное" / "автоматическое"

---

## 🔧 Файлы с изменениями

### Модели и миграции:
- `src/models/order.py` - обновлены модели `OrderDB`, `Order`, `CallStatusDB`
- `alembic/versions/003_refactor_manual_times.py` - новая миграция

### Сервисы:
- `src/services/db_service.py` - удалено `manual_call_time` из словаря заказа
- `src/services/route_optimizer.py` - добавлена логика учета `manual_arrival_time`
- `src/services/call_notifier.py` - обновлен `create_call_status` (защита от перезаписи)

### Обработчики:
- `src/bot/handlers/order_handlers.py`:
  - `_update_manual_arrival_time` - новый метод
  - `_update_manual_call_time` - обновлен
  - `show_order_details` - обновлено отображение
  - Обновлены вызовы `create_call_status`
  
- `src/bot/handlers/route_handlers.py`:
  - Обновлены вызовы `create_call_status` с новыми параметрами

---

## 📝 Для тестирования

### Сценарии:

1. **Установка времени звонка**
   - Выбрать заказ → "📞⏰ Время звонка" → ввести "12:50"
   - Проверить: отображается время звонка 12:50 и прибытия 13:30

2. **Установка времени прибытия**
   - Выбрать заказ → "⏰ Время прибытия" → ввести "13:40"
   - Проверить: отображается время прибытия 13:40 и звонка 13:00

3. **Реоптимизация с ручным временем**
   - Установить ручное время для заказа
   - Добавить новый заказ
   - Оптимизировать маршрут
   - Проверить: ручное время НЕ изменилось

4. **Добавление нового заказа**
   - После оптимизации с ручным временем
   - Добавить новый заказ
   - Оптимизировать
   - Проверить: новый заказ встроен в маршрут с учетом фиксированных точек

---

## ⚠️ Важно для деплоя

1. **Миграция БД**
   - Миграция `003` применится автоматически при запуске
   - Добавит поля `arrival_time` и `is_manual` в `call_status`
   - Удалит поле `manual_call_time` из `orders`

2. **Потеря старых данных**
   - Старые значения `orders.manual_call_time` будут удалены
   - Если нужно сохранить - сделать резервную копию БД

3. **Откат (downgrade)**
   - Миграция поддерживает откат (`downgrade`)
   - Вернет `manual_call_time` в `orders`
   - Удалит новые поля из `call_status`

---

## 📌 Итого

✅ Удалено дублирование `manual_call_time`
✅ Добавлена защита от перезаписи ручных установок
✅ Алгоритм оптимизации учитывает фиксированные времена
✅ Логичная архитектура хранения данных
✅ Гибкая установка времен (независимо друг от друга)

**Статус:** ✅ Рефакторинг завершен, готово к тестированию

