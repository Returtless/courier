# 🚀 Быстрый старт с тестированием

## ⚡ 3 команды для начала

```bash
# 1. Установить зависимости для тестов
pip install -r requirements-dev.txt

# 2. Запустить все unit-тесты
pytest tests/unit/ -v

# 3. Проверить покрытие кода
pytest tests/unit/ --cov=src --cov-report=term-missing
```

---

## ✅ Что уже протестировано

### 1. Модели данных (`test_models.py`)
- ✅ Создание заказов с разными данными
- ✅ Парсинг временных окон
- ✅ Хранение ручного времени прибытия
- ✅ Проверка отсутствия `manual_call_time` (удалено в рефакторинге)
- ✅ Создание и обновление `CallStatusDB`

### 2. Логика ручного времени (`test_manual_times.py`) 🔥
**Критические тесты после рефакторинга!**

- ✅ Создание `call_status` с ручным временем звонка
- ✅ Защита от перезаписи ручных установок при автоматической оптимизации
- ✅ Возможность обновления ручных времен вручную
- ✅ Хранение `manual_arrival_time` в таблице `orders`
- ✅ Флаг `is_manual` в `call_status`
- ✅ Расчеты времен (arrival ↔ call)
- ✅ Приоритеты при отображении

### 3. Уведомления о звонках (`test_call_notifier.py`)
- ✅ Создание и обновление `call_status`
- ✅ Отправка уведомлений в нужное время
- ✅ Подтверждение/отклонение звонков
- ✅ Повторные попытки звонков
- ✅ Обработка максимального количества попыток

### 4. Оптимизация маршрута (`test_route_optimizer.py`)
- ✅ Построение матриц расстояний/времени
- ✅ Оптимизация с одним/несколькими заказами
- ✅ Учет ручного времени прибытия при оптимизации 🔥
- ✅ Учет временных окон доставки
- ✅ Приоритет `manual_arrival_time` над `delivery_time_window`
- ✅ Расчеты времени с учетом времени обслуживания

---

## 🎯 Критические тесты (запустить первым делом!)

После рефакторинга ручного времени запустите:

```bash
pytest tests/unit/test_manual_times.py -v
```

**Ожидаемый результат:**
```
tests/unit/test_manual_times.py::TestManualCallTime::test_create_manual_call_time_new_record PASSED
tests/unit/test_manual_times.py::TestManualCallTime::test_manual_call_time_protected_from_auto_update PASSED
tests/unit/test_manual_times.py::TestManualCallTime::test_manual_call_time_can_be_updated_manually PASSED
tests/unit/test_manual_times.py::TestManualArrivalTime::test_manual_arrival_time_stored_in_orders PASSED
... и т.д.
```

---

## 📊 Запуск с покрытием

```bash
# Запуск с HTML-отчетом
pytest tests/unit/ --cov=src --cov-report=html

# Открыть отчет
# Windows:
start htmlcov/index.html

# Linux/Mac:
open htmlcov/index.html
```

---

## 🐛 Если тесты не проходят

### 1. Проверьте зависимости
```bash
pip install -r requirements.txt
pip install -r requirements-dev.txt
```

### 2. Проверьте БД
Тесты используют SQLite в памяти, но если есть проблемы:
```bash
# Удалите локальную БД (если есть)
rm data/courier.db

# Пересоздайте структуру
python -c "from src.database.connection import init_db; init_db()"
```

### 3. Запустите с подробным выводом
```bash
pytest tests/unit/test_manual_times.py -vv -s
```

### 4. Запустите один конкретный тест
```bash
pytest tests/unit/test_manual_times.py::TestManualCallTime::test_create_manual_call_time_new_record -vv
```

---

## 📝 Примеры использования

### Добавить свой тест

Создайте файл `tests/unit/test_my_feature.py`:

```python
import pytest
from src.models.order import Order

@pytest.mark.unit
def test_my_new_feature():
    """Описание теста"""
    # Arrange
    order = Order(
        customer_name="Тест",
        phone="+79991234567",
        address="Москва"
    )
    
    # Act
    result = order.customer_name
    
    # Assert
    assert result == "Тест"
```

Запустите:
```bash
pytest tests/unit/test_my_feature.py -v
```

### Использовать фикстуры

```python
@pytest.mark.unit
def test_with_db(test_db_session, sample_order_data):
    """Тест с БД и готовыми данными"""
    from src.models.order import OrderDB
    
    order = OrderDB(**sample_order_data, user_id=123, order_date=date.today())
    test_db_session.add(order)
    test_db_session.commit()
    
    saved = test_db_session.query(OrderDB).first()
    assert saved.customer_name == sample_order_data['customer_name']
```

---

## 🔥 Следующие шаги

После unit-тестов можно добавить:

### 1. Integration-тесты (2-3 дня)
```bash
mkdir tests/integration
# Тесты взаимодействия компонентов
```

### 2. E2E-тесты (2-3 дня)
```bash
mkdir tests/e2e
# Полные сценарии от начала до конца
```

### 3. CI/CD (1 день)
```bash
# Настроить GitHub Actions для автоматического запуска
```

---

## 📚 Дополнительная документация

- **`tests/README.md`** - подробная документация по тестам
- **`TESTING_STRATEGY.md`** - полная стратегия тестирования
- **`MANUAL_TIME_REFACTORING.md`** - документация рефакторинга

---

## ✨ Итого

✅ **52 unit-теста** охватывают критические компоненты
✅ **Логика ручного времени** полностью протестирована
✅ **Защита от регрессий** после рефакторинга
✅ **Готово к CI/CD** интеграции

**Запустите тесты и убедитесь, что все работает!** 🚀

