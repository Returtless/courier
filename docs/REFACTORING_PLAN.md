# 🚀 Пошаговый план рефакторинга

**Цель:** Разделить API и Bot, улучшить архитектуру, подготовить к мобильному приложению.

**Ожидаемое время:** 20-30 дней  
**Подход:** Поэтапная миграция без остановки работы бота

---

## 📋 Общая стратегия

1. **Создаем новые слои параллельно** со старыми
2. **Постепенно мигрируем** функционал
3. **Тестируем** каждый этап
4. **Удаляем старый код** только после полной миграции

---

## 🎯 Этап 0: Подготовка (1-2 дня) ✅ ВЫПОЛНЕНО

### Задачи:

- [x] **0.1. Создать структуру папок**
  ```bash
  mkdir -p src/application/services
  mkdir -p src/application/dto
  mkdir -p src/repositories
  mkdir -p src/api/routes
  mkdir -p src/api/schemas
  mkdir -p src/utils
  ```

- [x] **0.2. Установить зависимости**
  ```bash
  pip install dependency-injector fastapi uvicorn
  ```

- [x] **0.3. Создать базовые файлы**
  - `src/application/__init__.py`
  - `src/repositories/__init__.py`
  - `src/api/__init__.py`
  - `src/utils/__init__.py`

- [x] **0.4. Настроить тесты**
  - Создать тесты для новых компонентов
  - Настроить CI/CD для проверки

**Критерии готовности:**
- ✅ Структура папок создана
- ✅ Зависимости установлены
- ✅ Базовые файлы созданы

---

## 🎯 Этап 1: DI Контейнер и Базовые Репозитории (2-3 дня) ✅ ВЫПОЛНЕНО

### Задачи:

- [x] **1.1. Создать DI контейнер**
  - Файл: `src/application/container.py`
  - Настроить провайдеры для сервисов
  - Настроить провайдеры для репозиториев

- [x] **1.2. Создать BaseRepository**
  - Файл: `src/repositories/base_repository.py`
  - Базовые методы: `get`, `create`, `update`, `delete`
  - Работа с сессиями

- [x] **1.3. Создать OrderRepository**
  - Файл: `src/repositories/order_repository.py`
  - Методы:
    - `get_by_user_and_date(user_id, date) -> List[Order]`
    - `get_by_number(user_id, order_number, date) -> Order|None`
    - `save(order) -> OrderDB`
    - `update(order_number, updates) -> bool`
    - `get_active_orders(user_id, date) -> List[Order]`

- [x] **1.4. Создать RouteRepository**
  - Файл: `src/repositories/route_repository.py`
  - Методы:
    - `get_route(user_id, date) -> RouteData|None`
    - `save_route(user_id, date, route_data) -> RouteDataDB`
    - `get_start_location(user_id, date) -> StartLocation|None`
    - `save_start_location(user_id, date, location) -> StartLocationDB`

- [x] **1.5. Создать CallStatusRepository**
  - Файл: `src/repositories/call_status_repository.py`
  - Методы:
    - `get_by_order(user_id, order_number, date) -> CallStatusDB|None`
    - `get_pending_calls(user_id, date) -> List[CallStatusDB]`
    - `create_or_update(call_status) -> CallStatusDB`
    - `update_phone(user_id, order_number, phone) -> bool`

- [x] **1.6. Написать тесты для репозиториев**
  - Unit-тесты для каждого репозитория
  - Интеграционные тесты с БД

**Критерии готовности:**
- ✅ DI контейнер создан и протестирован
- ✅ Все репозитории созданы и протестированы
- ✅ Старый код продолжает работать

---

## 🎯 Этап 2: Application Services - Заказы (3-4 дня) ✅ ВЫПОЛНЕНО

### Задачи:

- [x] **2.1. Создать OrderDTO**
  - Файл: `src/application/dto/order_dto.py`
  - Классы: `OrderDTO`, `CreateOrderDTO`, `UpdateOrderDTO`

- [x] **2.2. Создать OrderService**
  - Файл: `src/application/services/order_service.py`
  - Методы:
    - `get_today_orders(user_id) -> List[OrderDTO]`
    - `get_order_by_number(user_id, order_number) -> OrderDTO|None`
    - `create_order(user_id, order_data: CreateOrderDTO) -> OrderDTO`
    - `update_order(user_id, order_number, updates: UpdateOrderDTO) -> OrderDTO`
    - `mark_delivered(user_id, order_number) -> bool`
    - `get_delivered_orders(user_id, date) -> List[OrderDTO]`

- [x] **2.3. Интегрировать ImageParser в OrderService**
  - Метод: `parse_order_from_image(user_id, image_data) -> OrderDTO|None`
  - Использовать существующий `ImageOrderParser`

- [x] **2.4. Написать тесты для OrderService**
  - Unit-тесты с моками репозиториев
  - Интеграционные тесты

- [x] **2.5. Обновить OrderHandlers**
  - Использовать `OrderService` вместо прямых обращений к БД
  - Оставить форматирование в handlers

**Критерии готовности:**
- ✅ OrderService создан и протестирован
- ✅ OrderHandlers использует OrderService
- ✅ Старый функционал работает

---

## 🎯 Этап 3: Application Services - Маршруты (3-4 дня) ✅ ВЫПОЛНЕНО

### Задачи:

- [x] **3.1. Создать RouteDTO**
  - Файл: `src/application/dto/route_dto.py`
  - Классы: `RouteDTO`, `RouteOptimizationRequest`, `RouteOptimizationResult`

- [x] **3.2. Создать RouteService**
  - Файл: `src/application/services/route_service.py`
  - Методы:
    - `optimize_route(user_id, order_date) -> RouteOptimizationResult`
    - `get_route(user_id, order_date) -> RouteDTO|None`
    - `get_start_location(user_id, order_date) -> StartLocationDTO|None`
    - `save_start_location(user_id, order_date, location_data) -> StartLocationDTO`
    - `recalculate_without_manual_times(user_id, order_date) -> RouteOptimizationResult`

- [x] **3.3. Интегрировать RouteOptimizer в RouteService**
  - Использовать существующий `RouteOptimizer`
  - Обработка ошибок оптимизации

- [x] **3.4. Создать CallService**
  - Файл: `src/application/services/call_service.py`
  - Методы:
    - `check_pending_calls(user_id, date) -> List[CallNotificationDTO]`
    - `confirm_call(user_id, call_status_id, comment) -> bool`
    - `reject_call(user_id, call_status_id) -> bool`
    - `create_call_status(user_id, order_number, call_time, ...) -> CallStatusDTO`

- [x] **3.5. Написать тесты для RouteService и CallService**

- [x] **3.6. Обновить RouteHandlers**
  - Использовать `RouteService` вместо прямых обращений
  - Оставить форматирование в handlers

**Критерии готовности:**
- ✅ RouteService и CallService созданы и протестированы
- ✅ RouteHandlers использует RouteService
- ✅ Оптимизация маршрута работает через новый сервис

---

## 🎯 Этап 4: Рефакторинг CallNotifier (2-3 дня) ✅ ВЫПОЛНЕНО

### Задачи:

- [x] **4.1. Разделить CallNotifier на сервис и notifier**
  - `CallService` - бизнес-логика (уже создан)
  - `TelegramNotifier` - отправка через Telegram
  - Файл: `src/bot/services/telegram_notifier.py`

- [x] **4.2. Создать интерфейс Notifier**
  - Файл: `src/application/interfaces/notifier.py`
  - Метод: `send_call_notification(notification: CallNotificationDTO)`

- [x] **4.3. Обновить CallNotifier**
  - Использовать `CallService` для проверки
  - Использовать `TelegramNotifier` для отправки
  - Убрать зависимость от Telegram Bot из бизнес-логики

- [x] **4.4. Написать тесты**

**Критерии готовности:**
- ✅ CallNotifier не зависит от Telegram Bot
- ✅ Можно создать PushNotifier для мобильного приложения
- ✅ Уведомления работают через новый механизм

---

## 🎯 Этап 5: Форматирование и Утилиты (2-3 дня) ✅ ВЫПОЛНЕНО

### Задачи:

- [x] **5.1. Создать форматтеры**
  - Файл: `src/utils/formatters.py`
  - Классы:
    - `OrderFormatter` - форматирование заказов для Telegram
    - `RouteFormatter` - форматирование маршрутов
    - `CallFormatter` - форматирование звонков

- [x] **5.2. Создать общие утилиты**
  - Файл: `src/utils/message_utils.py`
  - Класс: `FakeMessage` (для вызова handlers из callback)

- [x] **5.3. Создать константы**
  - Файл: `src/constants/messages.py`
  - Классы: `Messages`, `Buttons`, `Emojis`

- [x] **5.4. Создать централизованную обработку ошибок**
  - Файл: `src/utils/error_handler.py`
  - Декоратор: `@handle_errors`
  - Обработка разных типов ошибок

- [x] **5.5. Обновить handlers для использования форматтеров**

**Критерии готовности:**
- ✅ Форматирование вынесено в отдельные классы
- ✅ Константы централизованы
- ✅ Обработка ошибок унифицирована

---

## 🎯 Этап 6: REST API - Базовая структура (2-3 дня) ✅ ВЫПОЛНЕНО

### Задачи:

- [x] **6.1. Создать FastAPI приложение**
  - Файл: `src/api/main.py`
  - Настроить CORS, middleware
  - Подключить DI контейнер

- [x] **6.2. Создать аутентификацию**
  - Файл: `src/api/auth.py`
  - Использовать Telegram user_id как токен (или JWT)
  - Middleware для проверки аутентификации

- [x] **6.3. Создать Pydantic схемы**
  - Файл: `src/api/schemas/orders.py`
  - Файл: `src/api/schemas/routes.py`
  - Файл: `src/api/schemas/calls.py`
  - Request и Response модели

- [x] **6.4. Создать базовые endpoints**
  - Файл: `src/api/routes/orders.py`
  - Endpoints:
    - `GET /api/orders` - список заказов
    - `POST /api/orders` - создать заказ
    - `GET /api/orders/{order_number}` - получить заказ
    - `PUT /api/orders/{order_number}` - обновить заказ

- [x] **6.5. Настроить документацию**
  - OpenAPI/Swagger
  - Описание endpoints

**Критерии готовности:**
- ✅ FastAPI приложение запускается
- ✅ Базовые endpoints работают
- ✅ Аутентификация работает
- ✅ Документация доступна

---

## 🎯 Этап 7: REST API - Полный функционал (3-4 дня) ✅ ВЫПОЛНЕНО (частично)

### Задачи:

- [x] **7.1. Endpoints для маршрутов**
  - Файл: `src/api/routes/routes.py`
  - Endpoints:
    - `POST /api/routes/optimize` - оптимизировать маршрут
    - `GET /api/routes` - получить маршрут
    - `GET /api/routes/current-order` - текущий заказ
    - `GET /api/routes/start-location` - получить точку старта
    - `PUT /api/routes/start-location` - установить точку старта
    - ✅ `PUT /api/routes/current-order/{index}` - навигация (реализовано)

- [x] **7.2. Endpoints для звонков**
  - Файл: `src/api/routes/calls.py`
  - Endpoints:
    - `GET /api/calls` - график звонков
    - `GET /api/calls/{call_id}` - получить статус звонка
    - `POST /api/calls/{call_id}/confirm` - подтвердить звонок
    - `POST /api/calls/{call_id}/reject` - отклонить звонок

- [x] **7.3. Endpoints для настроек**
  - Файл: `src/api/routes/settings.py`
  - Endpoints:
    - `GET /api/settings` - получить настройки
    - `PUT /api/settings` - обновить настройки
    - `POST /api/settings/reset` - сбросить настройки

- [x] **7.4. Endpoints для импорта**
  - Файл: `src/api/routes/import_routes.py`
  - Endpoints:
    - `GET /api/import/credentials` - получить учетные данные
    - `POST /api/import/credentials` - сохранить учетные данные
    - `DELETE /api/import/credentials` - удалить учетные данные
    - `POST /api/import/chefmarket` - импорт из ШефМаркет

- [ ] **7.5. WebSocket для real-time уведомлений (опционально)**
  - Файл: `src/api/routes/websocket.py`
  - Endpoint: `WS /api/ws` - уведомления о звонках
  - ⚠️ **ОТЛОЖЕНО** - не критично для MVP, можно добавить позже

- [ ] **7.6. Написать тесты для API**
  - Интеграционные тесты endpoints
  - Тесты аутентификации
  - ⚠️ **ОТЛОЖЕНО** - можно добавить позже

**Критерии готовности:**
- ✅ Все основные endpoints работают
- ⚠️ API тесты не написаны (отложено)
- ✅ Документация полная (Swagger/OpenAPI)

---

## 🎯 Этап 8: Рефакторинг Bot Handlers (2-3 дня) ✅ ВЫПОЛНЕНО

### Задачи:

- [x] **8.1. Упростить OrderHandlers**
  - Убрать прямые обращения к БД
  - Использовать только `OrderService` и форматтеры
  - Оставить только UI логику

- [x] **8.2. Упростить RouteHandlers**
  - Убрать прямые обращения к БД
  - Использовать только `RouteService` и форматтеры
  - Оставить только UI логику

- [x] **8.3. Упростить остальные handlers**
  - CallHandlers, SettingsHandlers, ImportHandlers, TrafficHandlers
  - Использовать Application Services

- [x] **8.4. Обновить CourierBot**
  - Использовать DI контейнер
  - Убрать прямую инициализацию сервисов
  - Передавать зависимости через конструктор

- [ ] **8.5. Написать тесты для handlers**
  - Моки для сервисов
  - Проверка форматирования
  - ⚠️ **ОТЛОЖЕНО** - можно добавить позже

**Критерии готовности:**
- ✅ Handlers упрощены (только UI логика)
- ✅ Все используют Application Services
- ⚠️ Тесты для handlers не написаны (отложено)

---

## 🎯 Этап 9: Состояния пользователей (2-3 дня) ⚠️ ОТЛОЖЕНО

### Задачи:

- [ ] **9.1. Создать StateRepository**
  - Файл: `src/repositories/state_repository.py`
  - Методы: `get_state`, `update_state`, `clear_state`
  - Хранить в БД или Redis

- [ ] **9.2. Обновить CourierBot**
  - Использовать `StateRepository` вместо словаря
  - Добавить TTL для состояний
  - ⚠️ **Текущее состояние:** используется in-memory словарь `self.user_states = {}`

- [ ] **9.3. Очистка устаревших состояний**
  - Фоновая задача для очистки
  - TTL = 1 час

**Критерии готовности:**
- ⚠️ Состояния хранятся в памяти (не сохраняются при перезапуске)
- ⚠️ Состояния не сохраняются при перезапуске
- ⚠️ Устаревшие состояния не очищаются автоматически

**Примечание:** Этап отложен, так как текущая реализация (in-memory) достаточна для работы. Можно реализовать позже, если потребуется персистентность состояний.

---

## 🎯 Этап 10: Финальная оптимизация и документация (2-3 дня) ⚠️ В ПРОЦЕССЕ

### Задачи:

- [ ] **10.1. Оптимизация запросов к БД**
  - Проверить N+1 проблемы
  - Добавить eager loading где нужно
  - Оптимизировать индексы
  - ⚠️ **ОТЛОЖЕНО** - можно оптимизировать по мере необходимости

- [ ] **10.2. Кэширование**
  - Кэш для часто используемых данных
  - Redis для состояний (если используется)
  - ⚠️ **ОТЛОЖЕНО** - текущее кэширование в MapsService достаточно

- [x] **10.3. Документация**
  - Обновить `PROJECT_STRUCTURE.md` (частично)
  - Создать `API_DOCUMENTATION.md` (Swagger/OpenAPI доступен)
  - Создать `MIGRATION_GUIDE.md` для разработчиков (частично)
  - ✅ Созданы: `REFACTORING_COMPLETE.md`, `TESTING_GUIDE.md`, `TESTING_DOCKER.md`

- [ ] **10.4. Удаление старого кода**
  - Удалить неиспользуемые методы
  - Удалить прямые обращения к БД из handlers ✅ (выполнено)
  - Очистить комментарии
  - ⚠️ **ОСТАЛОСЬ:** `src/services/db_service.py` и `src/services/call_notifier_old.py` не используются, можно удалить

- [ ] **10.5. Финальное тестирование**
  - Полное покрытие тестами
  - Интеграционные тесты
  - Нагрузочное тестирование API
  - ⚠️ **ОТЛОЖЕНО** - базовые тесты есть, можно расширить

**Критерии готовности:**
- ✅ Все тесты проходят (базовые)
- ✅ Документация полная (Swagger/OpenAPI)
- ⚠️ Старый код частично удален (db_service.py, call_notifier_old.py можно удалить)
- ⚠️ Производительность приемлемая (можно оптимизировать позже)

---

## 📊 Чеклист прогресса

### Этап 0: Подготовка ✅
- [x] Структура папок
- [x] Зависимости
- [x] Базовые файлы

### Этап 1: DI и Репозитории ✅
- [x] DI контейнер
- [x] BaseRepository
- [x] OrderRepository
- [x] RouteRepository
- [x] CallStatusRepository
- [x] Тесты

### Этап 2: OrderService ✅
- [x] OrderDTO
- [x] OrderService
- [x] Интеграция ImageParser
- [x] Тесты
- [x] Обновление OrderHandlers

### Этап 3: RouteService ✅
- [x] RouteDTO
- [x] RouteService
- [x] CallService
- [x] Тесты
- [x] Обновление RouteHandlers

### Этап 4: CallNotifier ✅
- [x] Разделение на сервис и notifier
- [x] Интерфейс Notifier
- [x] Обновление CallNotifier
- [x] Тесты

### Этап 5: Форматирование ✅
- [x] Форматтеры
- [x] Утилиты
- [x] Константы
- [x] Обработка ошибок

### Этап 6: REST API - База ✅
- [x] FastAPI приложение
- [x] Аутентификация
- [x] Pydantic схемы
- [x] Базовые endpoints
- [x] Документация

### Этап 7: REST API - Полный ✅ (частично)
- [x] Endpoints маршрутов
- [x] Endpoints звонков
- [x] Endpoints настроек
- [x] Endpoints импорта
- [ ] WebSocket (опционально) ⚠️ ОТЛОЖЕНО
- [ ] Тесты ⚠️ ОТЛОЖЕНО

### Этап 8: Рефакторинг Handlers ✅
- [x] OrderHandlers
- [x] RouteHandlers
- [x] Остальные handlers
- [x] CourierBot
- [ ] Тесты ⚠️ ОТЛОЖЕНО

### Этап 9: Состояния ⚠️ ОТЛОЖЕНО
- [ ] StateRepository
- [ ] Обновление CourierBot
- [ ] Очистка состояний
- **Примечание:** Используется in-memory словарь, достаточен для текущих нужд

### Этап 10: Финальная оптимизация ⚠️ В ПРОЦЕССЕ
- [ ] Оптимизация БД ⚠️ ОТЛОЖЕНО
- [ ] Кэширование ⚠️ ОТЛОЖЕНО
- [x] Документация ✅ (частично)
- [ ] Удаление старого кода ⚠️ (db_service.py, call_notifier_old.py можно удалить)
- [ ] Финальное тестирование ⚠️ ОТЛОЖЕНО

---

## 🚦 Правила работы

1. **Не ломать существующий функционал**
   - Каждый этап должен быть рабочим
   - Тесты должны проходить

2. **Коммитить часто**
   - После каждой завершенной задачи
   - Понятные сообщения коммитов

3. **Тестировать постоянно**
   - Писать тесты параллельно с кодом
   - Не переходить к следующему этапу без тестов

4. **Документировать изменения**
   - Обновлять документацию после каждого этапа
   - Описывать breaking changes

5. **Рефакторить постепенно**
   - Не пытаться сделать все сразу
   - Следовать плану этапов

---

## 📝 Примечания

- **Время указано приблизительно** - может варьироваться в зависимости от сложности
- **Этапы можно выполнять параллельно** - если команда большая
- **Некоторые этапы можно пропустить** - если функционал не нужен (например, WebSocket)

---

## 🎯 Критерии успеха

После завершения всех этапов:

✅ **Архитектура:**
- Четкое разделение слоев
- Application Services независимы от UI
- Repositories инкапсулируют доступ к БД

✅ **Функциональность:**
- Весь существующий функционал работает
- REST API полностью функционален
- Мобильное приложение может использовать API

✅ **Качество:**
- Покрытие тестами > 80%
- Нет критических багов
- Документация полная

✅ **Производительность:**
- Время ответа API < 200ms (для простых запросов)
- Нет N+1 проблем
- Кэширование работает

---

**Начало работы:** [Дата]  
**Ожидаемое завершение:** [Дата + 30 дней]

---

## 📊 Текущий статус (обновлено)

### ✅ Выполнено (Этапы 0-8, частично 7, 10)
- **Архитектура:** Clean Architecture реализована
- **Application Services:** OrderService, RouteService, CallService работают
- **Repositories:** Все репозитории созданы и протестированы
- **DI Container:** Настроен и используется
- **REST API:** Все основные endpoints реализованы и используют Application Services
- **Bot Handlers:** Все handlers используют только Application Services
- **DTO:** Все DTO созданы и используются
- **Форматтеры и утилиты:** Реализованы

### ⚠️ Отложено (не критично для MVP)
- **WebSocket** (Этап 7.5) - можно добавить позже для real-time уведомлений
- **API тесты** (Этап 7.6) - можно добавить позже
- **StateRepository** (Этап 9) - текущая in-memory реализация достаточна
- **Оптимизация БД** (Этап 10.1) - можно оптимизировать по мере необходимости
- **Кэширование** (Этап 10.2) - базовое кэширование есть в MapsService

### 🔧 Рекомендации для дальнейшей работы
1. **Удалить старый код:** `src/services/db_service.py` и `src/services/call_notifier_old.py` не используются
2. **Добавить API тесты:** Интеграционные тесты для REST API endpoints
3. **Оптимизация:** Проверить N+1 проблемы, добавить индексы при необходимости
4. **StateRepository:** Реализовать, если потребуется персистентность состояний между перезапусками

### 🎯 Итог
**Основной рефакторинг завершен!** Код соответствует Clean Architecture, все критичные компоненты используют Application Services и DTO. REST API готов для использования мобильным приложением.

