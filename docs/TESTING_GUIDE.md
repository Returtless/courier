# Руководство по тестированию

## Варианты тестирования

### 1. 🐳 Тестирование в Docker (рекомендуется для продакшена)

Если у вас настроен PostgreSQL в Docker, используйте этот вариант:

#### Windows (PowerShell):
```powershell
# Убедитесь, что контейнеры запущены
docker-compose up -d

# Запустите тесты
.\scripts\test_in_docker.ps1
```

#### Linux/Mac:
```bash
# Убедитесь, что контейнеры запущены
docker-compose up -d

# Запустите тесты
chmod +x scripts/test_in_docker.sh
./scripts/test_in_docker.sh
```

#### Или вручную:
```bash
docker exec courier_bot python scripts/test_bot_functions.py
```

**Преимущества:**
- ✅ Использует ту же БД, что и продакшен (PostgreSQL)
- ✅ Все зависимости установлены
- ✅ Изолированная среда

### 2. 💻 Локальное тестирование с SQLite (быстрое)

Для быстрого локального тестирования без Docker:

```bash
python scripts/test_local_sqlite.py
```

**Преимущества:**
- ✅ Быстро (не нужен Docker)
- ✅ Не требует настройки PostgreSQL
- ✅ Создает отдельную тестовую БД (`test_courier_bot.db`)

**Недостатки:**
- ⚠️ Использует SQLite вместо PostgreSQL (могут быть различия)
- ⚠️ Нужны установленные зависимости локально

### 3. 🔧 Ручное тестирование в Telegram

После автоматических тестов проверьте основные функции в боте:

1. **Добавление заказа:**
   - Отправьте боту: `10:00-13:00 3258104 ул Манчестерская, д 3`

2. **Оптимизация маршрута:**
   - Установите точку старта (геопозиция или адрес)
   - Нажмите "▶️ Оптимизировать"

3. **Работа со звонками:**
   - Дождитесь уведомления о звонке
   - Подтвердите или отклоните звонок

4. **Импорт из ШефМаркет:**
   - Настройте учетные данные в меню "⚙️ Настройки"
   - Используйте команду `/import_orders`

## Что проверяют автоматические тесты

1. ✅ **Инициализация сервисов** - все сервисы создаются через DI контейнер
2. ✅ **OrderService** - создание, получение, обновление заказов
3. ✅ **RouteService** - сохранение и получение точки старта
4. ✅ **CallService** - создание и получение статусов звонков
5. ✅ **Инициализация CourierBot** - все handlers инициализируются корректно

## Устранение проблем

### Ошибка: "DATABASE_URL environment variable is not set"

**Решение:**
- Для Docker: убедитесь, что переменные окружения переданы в docker-compose
- Для локального тестирования: используйте `scripts/test_local_sqlite.py` (автоматически использует SQLite)

### Ошибка: "no such table: orders"

**Решение:**
- Убедитесь, что миграции применены
- В Docker: миграции применяются автоматически при запуске
- Локально: запустите `python scripts/migrate.py` или используйте `scripts/test_local_sqlite.py`

### Ошибка: "Container not running"

**Решение:**
```bash
docker-compose up -d
```

## Рекомендации

1. **Для разработки:** используйте `scripts/test_local_sqlite.py` - быстро и удобно
2. **Перед деплоем:** используйте тесты в Docker - проверяет реальную конфигурацию
3. **После рефакторинга:** обязательно запустите тесты перед коммитом

