# 📝 Руководство по миграции - Персональные настройки

## Обновление от предыдущей версии

Если у вас уже работает бот и есть база данных, после обновления кода нужно добавить таблицу настроек пользователей.

### Автоматическое обновление

При следующем запуске бота таблица `user_settings` будет создана автоматически благодаря `Base.metadata.create_all()` в `main.py`. Никаких дополнительных действий не требуется.

### Что было добавлено

#### Новая таблица `user_settings`:
```sql
CREATE TABLE user_settings (
    id INTEGER PRIMARY KEY,
    user_id INTEGER NOT NULL UNIQUE,
    call_advance_minutes INTEGER DEFAULT 10,
    call_retry_interval_minutes INTEGER DEFAULT 2,
    call_max_attempts INTEGER DEFAULT 3,
    service_time_minutes INTEGER DEFAULT 10,
    parking_time_minutes INTEGER DEFAULT 7,
    traffic_check_interval_minutes INTEGER DEFAULT 5,
    traffic_threshold_percent INTEGER DEFAULT 50,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

CREATE INDEX ix_user_settings_user_id ON user_settings(user_id);
```

#### Новый сервис:
- `src/services/user_settings_service.py` - управление настройками пользователей

#### Обновленные сервисы:
- `CallNotifier` - теперь использует настройки пользователя для времени звонка и повторных попыток
- `RouteOptimizer` - использует настройки для времени на точке
- `TrafficMonitor` - использует настройки для интервала проверки и порога уведомлений

#### Новое меню:
- Кнопка **⚙️ Настройки** в главном меню
- Интерфейс настройки всех параметров через Telegram

## Что изменилось для пользователей

### До обновления:
- Жестко заданные параметры (40 минут до приезда, 10 минут на точке, и т.д.)
- Все пользователи работали с одинаковыми настройками

### После обновления:
- Каждый пользователь может настроить параметры под себя
- Настройки сохраняются в базе данных
- При первом обращении создаются настройки по умолчанию
- Можно сбросить настройки к значениям по умолчанию

## Значения по умолчанию

- **Время звонка до приезда:** 10 минут (было 40 минут)
- **Интервал повторных звонков:** 2 минуты
- **Максимум попыток дозвона:** 3 раза
- **Время на точке:** 10 минут
- **Время на парковку:** 7 минут
- **Интервал проверки пробок:** 5 минут
- **Порог уведомлений о пробках:** 50%

> ⚠️ **Внимание:** Время звонка до приезда изменено с 40 на 10 минут. Пользователи могут настроить его по своему усмотрению.

## Проверка миграции

После запуска обновленного бота:

1. Откройте бота в Telegram
2. Нажмите **⚙️ Настройки** в главном меню
3. Убедитесь, что отображаются все параметры с значениями по умолчанию
4. Попробуйте изменить любую настройку

Если все работает - миграция прошла успешно!

## Откат (если нужно)

Если нужно откатиться к предыдущей версии:

```sql
DROP TABLE user_settings;
```

Затем верните старую версию кода.

