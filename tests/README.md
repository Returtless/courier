# 🧪 Unit-тесты для Courier Planning Bot

## 📁 Структура тестов

```
tests/
├── conftest.py                    # Общие фикстуры
├── unit/
│   ├── test_models.py            # Тесты моделей данных
│   ├── test_manual_times.py      # Тесты ручного времени (критично!)
│   ├── test_call_notifier.py     # Тесты уведомлений о звонках
│   └── test_route_optimizer.py   # Тесты оптимизации маршрута
└── README.md                      # Этот файл
```

---

## 🚀 Установка зависимостей

```bash
# Установка основных зависимостей
pip install -r requirements.txt

# Установка зависимостей для тестирования
pip install -r requirements-dev.txt
```

---

## ▶️ Запуск тестов

### Все unit-тесты
```bash
pytest tests/unit/
```

### Конкретный файл
```bash
pytest tests/unit/test_manual_times.py
```

### Конкретный тест
```bash
pytest tests/unit/test_manual_times.py::TestManualCallTime::test_create_manual_call_time_new_record
```

### С подробным выводом
```bash
pytest tests/unit/ -v
```

### С покрытием кода
```bash
pytest tests/unit/ --cov=src --cov-report=html
```
После выполнения откройте `htmlcov/index.html` в браузере.

### Только маркированные тесты
```bash
pytest -m unit  # Только unit-тесты
```

---

## 📊 Текущее покрытие

### Основные компоненты:

| Компонент | Покрытие | Статус |
|-----------|----------|--------|
| Models (Order, CallStatusDB) | ✅ Высокое | Готово |
| Manual Times Logic | ✅ Высокое | Готово |
| CallNotifier | ✅ Среднее | Готово |
| RouteOptimizer | ✅ Среднее | Готово |

---

## 🎯 Критические тесты

### После рефакторинга ручного времени

**Обязательно запустите:**

```bash
# Тесты логики ручного времени
pytest tests/unit/test_manual_times.py -v

# Ключевые тесты:
# ✅ Создание call_status с ручным временем
# ✅ Защита от перезаписи при автоматической оптимизации
# ✅ Хранение manual_arrival_time в orders
# ✅ Флаг is_manual в call_status
```

**Ожидаемый результат:** ✅ Все тесты проходят

---

## 📝 Фикстуры (conftest.py)

### Доступные фикстуры:

- `test_db_session` - Сессия БД в памяти (SQLite)
- `mock_db_session` - Мок сессии БД
- `freeze_time` - Фиксация текущего времени
- `sample_user` - Тестовый пользователь
- `sample_order_data` - Базовые данные заказа
- `mock_telegram_bot` - Мок телеграм бота
- `mock_maps_service` - Мок сервиса карт
- `mock_settings_service` - Мок сервиса настроек

### Использование:

```python
def test_my_feature(test_db_session, sample_order_data):
    # test_db_session уже готова к использованию
    order = OrderDB(**sample_order_data)
    test_db_session.add(order)
    test_db_session.commit()
```

---

## 🐛 Отладка

### Запуск с выводом print()
```bash
pytest tests/unit/ -s
```

### Остановка на первой ошибке
```bash
pytest tests/unit/ -x
```

### Запуск последних упавших тестов
```bash
pytest tests/unit/ --lf
```

### Вход в отладчик при ошибке
```bash
pytest tests/unit/ --pdb
```

---

## 📋 Чек-лист перед коммитом

- [ ] Все тесты проходят: `pytest tests/unit/`
- [ ] Покрытие > 70%: `pytest tests/unit/ --cov=src`
- [ ] Нет warnings: `pytest tests/unit/ --strict-warnings`
- [ ] Код отформатирован: `black src/ tests/`
- [ ] Нет lint ошибок: `flake8 src/ tests/`

---

## 🔄 CI/CD

Тесты автоматически запускаются при:
- Push в ветки `main`, `develop`, `feature/*`
- Создании Pull Request

См. `.github/workflows/tests.yml`

---

## 📚 Дополнительная документация

- `TESTING_STRATEGY.md` - Полная стратегия тестирования
- `MANUAL_TIME_REFACTORING.md` - Документация рефакторинга
- `pytest.ini` - Конфигурация pytest

---

## 💡 Советы

### Добавление нового теста

1. Создайте функцию с префиксом `test_`
2. Используйте декоратор `@pytest.mark.unit`
3. Используйте фикстуры из `conftest.py`
4. Добавьте описание в docstring

```python
@pytest.mark.unit
def test_my_new_feature(test_db_session, sample_order_data):
    """Описание того, что тестирует этот тест"""
    # Arrange
    order = OrderDB(**sample_order_data)
    
    # Act
    test_db_session.add(order)
    test_db_session.commit()
    
    # Assert
    saved = test_db_session.query(OrderDB).first()
    assert saved is not None
```

### Использование моков

```python
from unittest.mock import Mock, patch

@patch('src.services.call_notifier.get_db_session')
def test_with_mock(mock_session):
    session = Mock()
    mock_session.return_value.__enter__.return_value = session
    
    # Ваш тест
    ...
```

---

## 📞 Поддержка

Вопросы по тестам? Смотрите:
- `TESTING_STRATEGY.md` - полная документация
- `conftest.py` - примеры фикстур
- Существующие тесты - как шаблоны

